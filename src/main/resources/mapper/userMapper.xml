<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.shoppingShop.dao.UserDao">

    <select id="findUserById" parameterType="String" resultType="com.shoppingShop.domain.UserDto">
        SELECT user_id, user_pwd
        FROM user
        WHERE user_id = #{userId}
    </select>

    <select id="findByUserIdAndPassword" resultType="com.shoppingShop.domain.UserDto">
        SELECT * FROM user WHERE user_id = #{userId} AND user_pwd = #{userPwd}
    </select>

    <select id="selectUser" parameterType="String" resultType="com.shoppingShop.domain.UserDto">
        SELECT * FROM user WHERE user_id = #{userId}
    </select>
    <insert id="insertUser">
        INSERT INTO user (user_id, user_pwd, user_name, user_address, user_phone, user_email, created_at, updated_at)
        VALUES (#{userId}, #{userPwd}, #{userName}, #{userAddress}, #{userPhone}, #{userEmail}, #{createdAt}, #{updatedAt});
    </insert>

</mapper>