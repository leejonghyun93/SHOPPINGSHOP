<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.shoppingShop.mapper.CartMapper">
    <insert id="addCart" parameterType="com.shoppingShop.domain.CartDto">
        INSERT INTO cart (user_id, pro_id, cart_count, image_url, product_info, quantity, shipping_fee, total_price, unit_price)
        VALUES (#{userId}, #{proId}, #{cartCount}, #{imageUrl}, #{productInfo}, #{quantity}, #{shippingFee}, #{totalPrice}, #{unitPrice})
    </insert>

    <select id="getCartByUserId" parameterType="String" resultType="com.shoppingShop.domain.CartDto">
        SELECT * FROM cart WHERE user_id = #{userId}
    </select>

    <delete id="deleteCart" parameterType="Long">
        DELETE FROM cart WHERE cart_id = #{cartId}
    </delete>
</mapper>