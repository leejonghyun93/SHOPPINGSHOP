<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">


<!-- 사용자 ID로 장바구니 조회 -->
<mapper namespace="com.shoppingShop.mapper.CartMapper">

    <select id="selectCartByUserId" resultType="com.shoppingShop.domain.CartDto">
        SELECT *
        FROM cart
        WHERE user_id = #{userId}
    </select>

    <insert id="insertCart">
        INSERT INTO cart (user_id, pro_id, cart_count, image_url, product_info, pro_name, pro_color, pro_size, total_price)
        VALUES (#{userId}, #{proId}, #{cartCount}, #{imageUrl}, #{productInfo}, #{proName}, #{proColor}, #{proSize}, #{totalPrice})
    </insert>

    <delete id="deleteCart">
        DELETE FROM cart WHERE cart_id = #{cartId}
    </delete>

</mapper>

