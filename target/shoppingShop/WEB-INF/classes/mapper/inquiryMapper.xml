<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">


<mapper namespace="com.shoppingShop.dao.InquiryDao">

    <!-- 문의 리스트 가져오기 -->
    <select id="getInquiries" resultType="com.shoppingShop.domain.InquiryDto">
        SELECT *
        FROM product_inquiry
        WHERE pro_id = #{proId}
        ORDER BY created_at DESC
            LIMIT #{offset}, #{size}
    </select>

    <!-- 문의 개수 가져오기 -->
    <select id="getInquiryCount" resultType="int">
        SELECT COUNT(*)
        FROM product_inquiry
        WHERE pro_id = #{proId}
    </select>

    <!-- 문의 등록 -->
    <insert id="addInquiry" parameterType="com.shoppingShop.domain.InquiryDto">
        INSERT INTO product_inquiry (pro_id, content, created_at)
        VALUES (#{proId}, #{content}, NOW())
    </insert>
</mapper>