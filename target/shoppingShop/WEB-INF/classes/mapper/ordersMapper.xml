<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">


<mapper namespace="com.shoppingShop.dao.OrdersDao">

    <insert id="createOrder" parameterType="com.shoppingShop.domain.OrdersDto">
        INSERT INTO orders (user_id, cart_id, od_status, quantity, unit_price, shipping_fee, od_total_price, created_at)
        VALUES (#{userId}, #{cartId}, #{odStatus}, #{quantity}, #{unitPrice}, #{shippingFee}, #{odTotalPrice}, NOW());
    </insert>

    <select id="getOrderHistoryByUserId" parameterType="string" resultType="com.shoppingShop.domain.OrdersDto">
        SELECT *
        FROM orders
        WHERE user_id = #{userId}
    </select>

</mapper>